# Kotlin compiler daemon memory settings
kotlin.daemon.jvmargs=-Xmx2048m -Xms512m

# Gradle daemon memory settings
org.gradle.jvmargs=-Xmx2048m -Xms512m

# Enable Gradle parallel builds
org.gradle.parallel=true

# Enable Gradle caching
org.gradle.caching=true

# Configure Gradle daemon
org.gradle.daemon=true
